import {Subject} from './subject';

export class Test {
  id: number;
  subjectId: number;
  nameTest: string;
  dueDateTime: number;
  estimatedTime: number;
  questionsAmount: number;
  maxMark: number;
  isOpen: boolean;
}
